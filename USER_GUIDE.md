# Dailog 使用手册

本文介绍 Dailog 的日常使用、AI 配置、本地 MCP 接入和数据备份方式。

## 1. 启动软件

### Windows Release

1. 下载 Dailog Windows ZIP 并解压。
2. 双击 `Dailog.exe`。
3. 软件启动后会显示桌面浮窗，并在系统托盘显示 Dailog 图标。

浮窗适合持续记录事项。控制台用于查看整个月份、编辑详情、生成汇报和调整设置。

### 打开控制台

右击系统托盘中的 Dailog 图标，选择打开控制台。关闭控制台不会删除或改变记录；退出 Dailog 后，本地 MCP 也无法继续访问应用数据。

## 2. 使用桌面浮窗

### 新增当天记录

1. 点击浮窗右上角的“+”。
2. 如果当天还没有记录，Dailog 会创建当天日期卡片并定位到该卡片。
3. 如果当天已经存在记录，Dailog 会定位并短暂高亮当天卡片。
4. 在卡片中填写事项，保存后事项默认是“进行中”。

每个日期卡片最多直接展示四条事项。事项较多时，可以在卡片内容区域滚动查看；卡片底部会显示事项总数。

### 修改事项状态

每条事项后面都有状态按钮：

- 进行中：事项仍在推进。
- 停滞：事项暂时没有进展。
- 完成：事项已经完成。
- 放弃：事项不再继续推进。

选中的状态会使用对应颜色显示。状态会同时同步到控制台和日报生成数据中。

### 日期卡片菜单

右击日期卡片可以执行以下操作：

- 查看日报：打开当天详情。
- 生成日报：根据当天事项生成日报。
- 重新生成日报：使用最新事项和进展覆盖当天日报。
- 删除日期：删除该日期及其中的事项、进展和日报。

删除操作不会提供恢复功能。需要保留数据时，请先导出记录。

## 3. 使用控制台

### 按月份查看

控制台首页按月份展示日期。使用左右箭头切换月份：左侧为上一个月，右侧为下一个月。没有记录的日期保持空白，不会显示多余提示。

点击日期卡片可以打开详情弹窗。日期卡片内会显示事项摘要、状态和日报状态。长文本在日期卡片中会按界面规则截断，完整内容可在详情中查看。

### 查看和编辑日期详情

详情弹窗分为浏览模式和编辑模式：

- 浏览模式：查看日期、事项、每条事项的状态、文本进展和当日日报。
- 编辑模式：修改事项内容、状态、进展和当日日报。
- 事项较长时，点击事项可以展开完整编辑区域。
- 子项只记录文本进展，不单独设置状态。
- 保存后，内容会同步到浮窗和其他窗口。

当日日报尚未生成时，日报区域保持为空。当天生成过日报后，日期卡片会显示绿色日报标记。

### 生成单日日报

在控制台中右击日期卡片，选择“生成日报”或“重新生成日报”。也可以在浮窗中右击日期卡片执行相同操作。

生成日报需要满足：

- 当天存在至少一条事项。
- AI 接口地址、API Key 和模型名称已配置。
- 模型服务可以正常访问。

同一天多次生成时只保留最新日报。日期卡片只要存在日报记录，就显示绿色标记。

### 生成区间汇报

1. 在控制台日期区域按住鼠标拖动，选中起始日期和终止日期。
2. 右击选区，选择“生成区间汇报”。
3. Dailog 会整合选中范围内的事项、状态和文本进展，交给 AI 生成汇报。
4. 生成后的汇报按生成时间倒序排列，最新汇报显示在最前面。

点击选区外的区域可以取消选区。右击选区内仍然可以选择生成区间汇报或取消选区。

区间汇报按生成时间所属的年份和月份分组，例如 `2026年9月`。跨月区间按照约定归入起始月份。

右击区间汇报条目可以删除该条汇报，点击条目可以重新打开并查看完整内容。

## 4. 配置 AI

打开控制台的“设置”，填写以下内容：

### 模型配置

- **接口地址**：OpenAI API 兼容接口的 API 根地址。
- **API Key**：模型服务商提供的访问密钥。
- **模型名称**：模型服务商支持的模型名称。

Dailog 支持使用 OpenAI Chat Completions 兼容格式的模型服务。配置完成后点击“测试连接”。如果测试失败，请检查接口地址是否正确、模型名称是否存在，以及网络和 API Key 是否可用。

### 日报设置

- **日报复杂度**：选择“简单”“适中”或“较长”，用于控制输出篇幅。
- **日报要求**：可以填写固定模板，也可以填写自然语言要求。例如：

```text
只输出一段话，总结今日工作，不新增栏目。
```

日报和区间汇报都会遵守这项要求，并把事项状态和文本进展作为约束输入。

### 自动生成时间

设置日报生成时间后，Dailog 只会在软件运行期间到达该时间时尝试生成当日日报。软件未启动、电脑处于睡眠状态、网络不可用或模型请求失败时，不会补发绿色日报标记；用户可以启动软件后手动生成。

### 开机自启

开启“开机自动启动 Dailog”后，Dailog 会在用户登录 Windows 后启动。关闭该选项即可取消开机自启。

### MCP 访问开关

开启“允许本地 MCP 访问 Dailog 数据”后，支持 MCP 的本地 Agent 才能调用 Dailog。关闭开关后，MCP 子进程会拒绝访问。

## 5. 配置本地 MCP

MCP 是 Dailog 面向本地 Agent 的接口。它使用 stdio，不开放网络端口，也不是 Dailog 启动后一直监听的常驻服务。

### 配置步骤

1. 启动 Dailog。
2. 打开控制台的“设置”。
3. 开启“允许本地 MCP 访问 Dailog 数据”。
4. 点击“复制 MCP 客户端配置”。
5. 将配置粘贴到 Agent 的 MCP 配置中。
6. 重启或重新连接 Agent。

### 可执行操作

Agent 可以通过 MCP：

- 检查 Dailog 是否运行以及 MCP 是否可用。
- 查询某天的事项、状态、进展和日报。
- 查询日期范围内的记录。
- 查询近期未完成事项。
- 新增事项。
- 修改事项状态。
- 补充事项进展。
- 生成单日日报。
- 生成区间汇报。

事项转记、周报整理等更复杂的工作流可以由 Agent 组合这些基础能力完成。MCP 调用生成日报或区间汇报时，会复用 Dailog 设置中的模型、日报要求和复杂度配置。

### MCP 故障排查

- 如果 Agent 无法连接，确认 Dailog 主程序正在运行。
- 确认设置页的 MCP 开关已开启。
- 使用设置页重新复制当前 MCP 配置，不要继续使用旧安装目录中的路径。
- 确认 Agent 使用的是 stdio 配置，而不是 HTTP 地址。
- 在 Dailog 退出后，MCP 调用失败属于正常行为。

## 6. 导入和导出数据

设置页提供四种 JSON 数据操作：

| 操作 | 作用 |
| --- | --- |
| 记录导出 | 导出事项、进展、日报和区间汇报，不包含设置 |
| 全部数据导出 | 导出记录和所有设置，可能包含 API Key |
| 数据导入 | 覆盖本地记录，保留当前设置 |
| 全量导入 | 覆盖本地记录和全部设置 |

导入前建议先导出当前数据。全量导入前尤其要确认文件来源可信，因为它会覆盖 AI 接口、模型名称、API Key 和其他设置。

## 7. 数据与隐私

- Dailog 数据默认保存在本机 SQLite 数据库中。
- Dailog 不提供云端同步，也不会自动上传本地全部数据。
- 只有在生成日报、生成区间汇报或调用相关 MCP 工具时，必要内容才会发送到用户配置的模型接口。
- API Key 不会写入 Git 仓库，但全部数据导出文件可能包含 API Key。
- 不要把全量导出文件上传到公开仓库或发送给不可信的人。

## 8. 常见问题

### 浮窗没有显示

确认 Dailog.exe 仍在运行，并检查系统托盘是否有 Dailog 图标。可以右击托盘图标重新打开浮窗。

### 日报生成失败

先点击“测试连接”。如果测试通过但日报失败，请检查当天是否存在事项，以及模型是否返回了空内容。

### 导入导出失败

请在桌面客户端中执行导入导出，并确认选择的是 JSON 文件。全量导入文件必须是 Dailog 导出的完整数据文件。

### MCP 调用失败

确认 Dailog 正在运行、MCP 开关已开启，并重新复制 MCP 配置。MCP 不支持在 Dailog 退出后继续访问数据。

## 9. 反馈

使用问题或功能建议请提交到 GitHub Issues：

<https://github.com/wangyuhao07/Dailog/issues>
